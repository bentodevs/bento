name: Run ESLint

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  eslint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Modules
        run : |
          npm ci --only=dev
      - name: Run ESLint
        run: |
          npm run lint